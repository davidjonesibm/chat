# General Guidelines

- This is a pnpm workspace monorepo with three packages: `@chat/frontend`, `@chat/backend`, and `@chat/shared`
- Run tasks via `pnpm --filter <package> <script>` or `pnpm -r run <script>` for all packages
- Always read `docs/ARCHITECTURE.md` before planning or implementing any feature
